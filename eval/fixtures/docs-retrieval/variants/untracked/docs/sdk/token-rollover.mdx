import { Preview } from "../../components/Preview"

# SDK token rollover

## Overlap window

<Preview feature="token-rollover">
  Atlas Relay accepts both the old SDK token and the replacement token for a 10-minute overlap window. Revoke the old token after every client has received the replacement.
</Preview>

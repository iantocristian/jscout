import { CompatibilityNote } from "../../components/CompatibilityNote"

# Filter expressions

## Supported operators

<CompatibilityNote product="Atlas Relay">
  A subscription filter supports `eq`, `neq`, `in`, and `prefix`. Compound filters join clauses with `and`; `or` is not supported.
</CompatibilityNote>

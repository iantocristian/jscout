# Relay v1 authentication

## API-key header

The Relay v1 publishing endpoint requires the `X-Atlas-Key` HTTP header. Its value is the workspace API key.

This versioned instruction applies only to Relay v1 publishers.

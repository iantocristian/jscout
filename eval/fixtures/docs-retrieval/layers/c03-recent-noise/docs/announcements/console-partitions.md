# Console refresh

## Partition activity panel

The Atlas Relay console now groups recent events by partition and displays partition-assignment activity for each route key. Operators can filter the panel by route key, stream, and partition number.

The panel observes assignment results; it does not define or change the partition-assignment algorithm.

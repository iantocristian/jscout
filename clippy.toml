# Words that look like identifiers to clippy::doc_markdown but are prose:
# product and specification names that should render as text, not code.
# ".." keeps clippy's own default list rather than replacing it.
doc-valid-idents = ["SQLite", "CommonJS", "JavaScript", "TypeScript", "WebAssembly", ".."]
